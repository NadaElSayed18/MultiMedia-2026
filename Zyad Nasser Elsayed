# ============================
# Task 1: دالة الميزانية (Budget)
# ============================

def budget_affordable():
    # عرّفت دالة اسمها budget_affordable عشان أتعامل مع الميزانية وسعر الصنف

    try:
        budget = int(input("أدخل ميزانيتك الكاملة (كعدد صحيح): ").strip())
        # هنا بسأل المستخدم يدخل الميزانية، وبحوّلها لـ int (عدد صحيح)
        
        cost = int(input("أدخل تكلفة الصنف (كعدد صحيح): ").strip())
        # هنا بسأله يدخل تكلفة الصنف، وبرضو بحوّلها لـ int
    except ValueError:
        print("الرجاء إدخال أرقام صحيحة (integers).")
        # لو المستخدم كتب حاجة مش رقم، البرنامج هيطبع رسالة خطأ
        return
        # وبيرجع من الدالة عشان ميكملش باقي الكود

    if cost <= budget:
        # هنا بشوف لو الصنف تمنه أقل أو يساوي الميزانية
        remaining = budget - cost
        # بحسب الفرق بينهم عشان أعرف كام باقي من الميزانية
        print(f"الصنف ميسور. المتبقي من الميزانية: {remaining}")
        # بطبع رسالة إن الصنف ميسور وكمان قد إيه باقي
    else:
        needed = cost - budget
        # لو الصنف أغلى من الميزانية، بحسب قد إيه ناقصني
        print(f"الصنف غالي. تحتاج {needed} إضافية لشراءه.")
        # وبطبع رسالة إن الصنف غالي وقد إيه محتاج فلوس زيادة


# ============================
# Task 2: دالة Title Case
# ============================

def title_case():
    # هنا عرّفت دالة اسمها title_case عشان تحول الجملة لـ Title Case

    sentence = input("أدخل جملة لتحويلها إلى Title Case: ")
    # بطلب من المستخدم يدخل جملة

    titled = sentence.title()
    # بستخدم دالة title() الجاهزة في بايثون اللي بتخلي أول حرف في كل كلمة كبير

    print("الجملة الأصلية:", sentence)
    # بطبع الجملة اللي المستخدم كتبها

    print("بعد التحويل (Title Case):", titled)
    # وبطبع الجملة بعد ما اتعدلت بالـ Title Case


# ============================
# Task 3: دالة جمع ومقارنة مع 100
# ============================

def sum_compare_100():
    # هنا عرّفت دالة بتجمع رقمين عشريين وتقارن الناتج بالعدد 100

    try:
        a = float(input("أدخل الرقم الأول (float): ").strip())
        # بسأل المستخدم يدخل الرقم الأول، وبحوّله لـ float (يعني ممكن يكون فيه كسور)

        b = float(input("أدخل الرقم الثاني (float): ").strip())
        # نفس الكلام للرقم التاني
    except ValueError:
        print("الرجاء إدخال أعداد عشرية صحيحة (floats).")
        # لو المستخدم كتب حاجة مش رقم، البرنامج هيقول كده
        return
        # ويرجع من الدالة بدون ما يكمل

    total = a + b
    # هنا بحسب مجموع الرقمين وبخزّنه في متغير اسمه total

    print(f"المجموع = {total}")
    # بطبع المجموع للمستخدم

    if total >= 100:
        # لو المجموع أكبر أو يساوي 100
        print("النتيجة: المجموع أكبر أو يساوي 100.")
        # بطبع الرسالة المناسبة
    else:
        # لو المجموع أقل من 100
        print("النتيجة: المجموع أصغر من 100.")
        # بطبع الرسالة التانية المناسبة


# ============================
# Menu: قائمة لتشغيل المهام بسهولة
# ============================

def main_menu():
    # هنا عملت دالة اسمها main_menu فيها منيو بسيطة للمستخدم يختار منها

    while True:
        # استخدمت while True عشان تفضل المنيو شغالة لحد ما المستخدم يختار الخروج

        print("\nاختر المهمة لتشغيلها:")
        # بطبع رسالة للمستخدم فيها الاختيارات

        print("1 - تمويل / ميزانية (Task 1)")
        print("2 - تحويل إلى Title Case (Task 2)")
        print("3 - جمع ومقارنة بـ100 (Task 3)")
        print("0 - خروج")
        # هنا بعرض كل الاختيارات اللي يقدر المستخدم يختار منها

        choice = input("اختيارك: ").strip()
        # بسأل المستخدم يكتب اختياره

        if choice == "1":
            budget_affordable()
            # لو اختار 1، يشغل دالة الميزانية
        elif choice == "2":
            title_case()
            # لو اختار 2، يشغل دالة الـ Title Case
        elif choice == "3":
            sum_compare_100()
            # لو اختار 3، يشغل دالة الجمع والمقارنة
        elif choice == "0":
            print("وداعاً!")
            break
            # لو اختار 0، البرنامج يطبع وداعاً ويخرج من اللوب
        else:
            print("اختيار غير صالح، حاول مرة أخرى.")
            # لو كتب رقم غلط، البرنامج يقول إن الاختيار غير صالح


# ============================
# تشغيل البرنامج من هنا
# ============================

if __name__ == "__main__":
    main_menu()
    # هنا يعني لما أشغل الملف ده مباشرة، يبدأ يشغل المنيو الرئيسية
